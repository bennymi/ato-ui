import type { ExampleHighlights, Data } from "../types";

const highlights: ExampleHighlights = {
    main: {
        'app.svelte': {
            lines: '',
            words: [{ strings: ['$trigger', 'use:trigger', '$item', 'use:item', '$menu', 'use:menu', '$checkboxItem', 'use:checkboxItem', '$subTrigger', 'use:subTrigger', '$subOpen', '$open', '$radioGroup', 'use:radioGroup', '$radioItem({ theme })', 'use:radioItem', '$isChecked(theme)', '$subArrow', 'use:subArrow', '$separator', 'use:separator'], id: 'builder' }],
        }
    }
}


export const dropdownMenuHeadlessData: Data = {
    highlights,
}