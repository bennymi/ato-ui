import type { ExampleHighlights, Data } from "../types";

const highlights: ExampleHighlights = {
    main: {
        'app.svelte': {
            lines: '',
            words: [{ strings: ['trigger', 'overlay', 'content', 'close', '$open'], id: 'builder' }],
        }
    }
}


export const dialogHeadlessData: Data = {
    highlights,
}